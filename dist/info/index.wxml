<wxs src="../wxs/utils.wxs" module="utils" />
<view wx:if="{{ info !== null && info !== '' || dot }}" class="{{customClass}} custom-class van-info {{ utils.bem('info', { dot }) }}  {{ type === 'num'? 'van-info--num':''}}" style="{{ customStyle }}">
  {{ dot ? '' : info }}
</view>